// The webhooks feature's full public surface. To erase it: delete this
// folder and remove its one import line + usage from App.tsx. The panel
// talks to /api/v1/webhooks through the generated typed client only.
export { WebhooksPanel } from "./WebhooksPanel.js";
